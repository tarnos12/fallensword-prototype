# Fallen Immortal — Working Notes for Claude Code

This file is the handoff/status doc. **Keep it current: update it in the same commit as every completed task**, so any Claude Code (or the author) can resume cold. Author is Mariusz (GitHub `tarnos12`).

> **⚑ Working in parallel? Read this first.** Multiple Claude Code sessions may run at once. Before writing any code, `git fetch origin coordination` and read **`TASKS.md`** on the `coordination` branch — it's the shared task board. Claim an `AVAILABLE` task there (follow its Claim protocol — the claim is an atomic push race, first push wins) so two sessions never build the same thing or stomp each other's files. Leave handoff notes for other sessions in that file too. `master` stays code-only; `coordination` is the meeting point.

## What this is

A browser-based, offline-first, FallenSword-inspired stat-math dungeon-crawler RPG with **xianxia** flavor (cultivators, realms, spirit stones, sects), built with a designed path to (fake, then real) multiplayer. Pure HTML/JS/CSS, no build step, no framework. Full design in [GDD_Staged_Roadmap.md](GDD_Staged_Roadmap.md) — it is the source of truth for scope and staged roadmap.

Public repo: https://github.com/tarnos12/fallensword-prototype (branch `master`).

## Run & test

- Dev server: `npx serve -l 8123 .` (config in `.claude/launch.json`, name `fallen-immortal`). Use the preview tools, not Bash, to run/verify.
- **Always end a change summary with the test link: http://localhost:8123** — **except in cloud/remote sessions** (Claude Code on the web), where the server runs in a throwaway container the author can't reach. There, skip the link: verify in-container (headless Node sims + real-Chromium/Playwright DOM checks) and tell the author how to run it themselves (`git checkout <branch> && npx serve -l 8123 .`, open `http://localhost:8123` on **their** machine — ES modules need a static server, not `file://`).
- Screenshots via the preview tool tend to time out in this environment — verify via DOM inspection (`preview_eval`, `preview_snapshot`, `preview_inspect`) instead.
- Balance is tuned **headless**: import `resolveCombat`/`generateItem`/etc. in Node and run bulk sims (see scratchpad `*-sim.mjs` patterns). No browser needed for tuning.

## Current status (update this section every commit)

**Stage 2 (Demo) — in progress.** Stages 0 and 1 complete.

Done:
- Stage 0: 10×10 grid, combat resolver, Qi.
- Stage 1: save/load, cultivation stages + stat allocation, gear (rarity/durability/repair), 5-quest chain, bestiary kill tracking.
- Instant combat (default) with a turn-by-turn playback toggle.
- Full rarity ladder (Common→Uncommon→Rare→Epic→Legendary→Mythic); **unique item types per rarity** (name signals tier, no prefixes); rarity gates attribute count; random drops cap at Rare.
- FallenSword-style icon-grid inventory/equipment with tooltips + right-click menu; 2-stats-per-row character sheet.
- **Multi-zone world** (`map.js` ZONES): Azuremist Vale → Cindervein Gorge, connected by a stage-gated portal; realm ladder Qi Condensation → Foundation Establishment with a realm-barrier XP spike; 3 Foundation-tier creatures; 9-quest chain across both zones. Save v2 with lossless v1 migration.
- **Techniques** (`techniques.js`): 8 across Offense/Defense/Special, 2–3 tier prereq tree, learned with banked points, channelled for timed Qi-cost percentage buffs; wall-clock expiry; routed through `effectiveStats`.
- **Bestiary + Spirit Cards** (`cards.js`): Beast Codex modal (📖 button) over the existing kill data with progressive disclosure (10 kills → combat stats, 50 → drop table, 100 → card drop chance + mastery mark); a codex entry is created on first encounter (inspect **or** fight). Spirit Cards — one `cardId` per creature, a separate-from-loot drop roll, duplicate-upgrade (L1→5, beyond-max → spirit-stone payout). Combat-stat cards (attack/damage/armor/hp) plug into the `effectiveStats` pipeline; meta cards drive the Qi cap (`maxQi`) and passive spirit-stones/hour (`tickStones`, wall-clock/offline like Qi regen). Each codex entry shows the creature's card silhouetted until owned, then with its level. `bonusType` enum is extensible (XP%, repair discount, inv slots… flagged for later). GDD §7.
- **Treasure Pavilion** (`market.js` + `personas.js`): fake-multiplayer auction house (🏛 button) behind a `MarketProvider` interface (`getListings`/`buyNow`/`listItem`/`collectMailbox` + `tick`) — a 2.0 `NetworkMarketProvider` drops in with the same shape. 72-persona roster generated once from a fixed seed (deterministic, so it isn't persisted). NPC listings rotate on a timer (refresh every ~3 min, 20-min TTL), pulled from the loot tables (capped at Rare), priced around a fair value with deliberate ±noise (bargains ↔ rip-offs). Buy Now only. Player selling escrows the item and resolves asynchronously — sell/no-sell decided at list time from price competitiveness, proceeds/returns land in a **mailbox** (collect later; full pack blocks item collection). Wall-clock `tick` resolves sales offline. Tabbed modal: Browse / Sell / My Listings / Mailbox, with a mailbox badge. GDD §6.7.
- **Sect / Warband stub** (`guild.js`): hireable NPC disciples (⛩ Sect button) behind a `GuildProvider` interface (`getMembers`/`getRecruits`/`hire`/`dismiss`/`buffs`) — a 2.0 `NetworkGuildProvider` drops in with the same shape. Recruits come from the shared `personas.js` roster (same cast as the Pavilion); each persona's specialty is derived deterministically from its id, buff scales with its level. 4 specialties (War → battle XP%, Merchant → battle spirit-stone%, Alchemy → passive stones/hour, Meditation → max Qi). Sect capacity 3; hiring costs spirit stones, dismissing is free. Buffs are **economy-category** and plug into existing hooks: `maxQi` (Qi cap), `tickStones` (passive income), and kill rewards in `attack()` (XP%/stone%). Members persist on `player.guild`. Combat-stat sect buffs are a later extension (would plug into `effectiveStats` like cards). GDD §3/§4.3/§9.1.
- **Onboarding / tutorial** (`tutorial.js`): a first-run, dismissible 9-step overlay (❔ Help button to reopen) that walks a new player through movement/Qi, inspect-then-attack, the character sheet & stat points, gear, techniques, and the Codex/Pavilion/Sect systems — spotlighting the real UI element each step. Self-contained: builds its own DOM (no `index.html` markup, never touches `ui.js`); "seen" state is its own localStorage key (not the save schema). Stage 2 exit criterion "a stranger plays with zero explanation."
- **Combat Sets / loadouts** (`loadouts.js`): save the currently-equipped artifacts as a named set and swap between sets in one click (GDD §6.2). Sets live on `player.loadouts` (persist with the save; no `save.js` change); applying re-equips through the existing `items.js` equip/unequip, skipping sold/lost pieces and reporting them. Cap of 4 sets; re-saving a name overwrites. Self-contained view: a "Combat Sets" panel-box inserted after the gear box (no `index.html` markup, never touches `ui.js`). Game-layer wrappers `saveLoadoutAction`/`applyLoadoutAction`/`deleteLoadoutAction`.
- **Save export / import** (`save.js` additive `exportSave`/`importSave`): back up / restore a save without an account (💾 Backup / Restore modal). `exportSave()` wraps the raw localStorage blob as `FIMMORTAL-SAVE-v1:` + UTF-8-safe base64 (survives copy-paste, non-ASCII names intact). `importSave(str)` accepts the wrapped form or a bare JSON blob, validates (recognized `version` 1/2 + a `player` object; rejects garbage/future-version/malformed), writes to localStorage, and lets the next boot run the normal v1→v2 migration — it does **not** reload; the caller does. `save.js` stays DOM-free: the modal, clipboard, file download (`fallen-immortal-save.txt`) and file-load live in `main.js` `initBackup()`. GDD §4.4.
- **Legendary boss** (`boss.js`): the **Ancient Terror** — *Xuanming*, a hand-authored calamity beast (GDD §3 world-events stub, §9.1). A *scheduled solo encounter*, deliberately NOT in `CREATURE_TYPES`/any zone spawn table: it manifests at a fixed lair tile (Cindervein Gorge `(9,9)`, band 3), gated behind reaching **Foundation Establishment 1** (`minStage 10`), and — once slain — recedes on a **wall-clock cooldown** (stub 30 min) before re-manifesting for repeat runs. Fixed hand-tuned stat block (ATK 39 · DEF 33 · DMG 25 · ARM 16 · HP 332 @ Lv 15; headless-tuned so under-geared = crushed, FE3 = a real win/draw/loss gamble, maxed+buffed = reliable win). Reuses everything — it's an Actor-shaped monster on its tile, so inspect/combat/playback/gear-degrade/bestiary all work unchanged; only the **reward path branches** in `attack()`: the game's first **Epic (chance Legendary) drop** + a potent boss **Spirit Card** (`card_ancientTerror`, +3 Damage/level, 50% drop). Manifest/cooldown/reward logic lives in `boss.js` (`maybeManifestBoss`/`onBossDefeated`/`bossLairStatus`); `game.js` calls it on move/travel/load. `player.boss` (`{defeatedAt, defeats}`) back-filled in `createGame`. UI: pulsing ☠ lair marker on the map, boss-styled tile row with a **Challenge** button, a lair note (locked/cooldown) when dormant, a gold-on-ember legendary victory banner, and a dedicated **Legendary Calamity** entry in the Beast Codex (counts toward beast/card totals). GDD §9.1.
- **Profile & Rivals feed** (`profile.js`): a read-mostly profile modal (👤 Profile & Rivals button) behind a `ProfileProvider` interface (`summary`/`buffs`/`rivals`/`rivalCandidates`/`addRival`/`removeRival`/`recentlyActive`) — a 2.0 network impl fills the same shape from real players. Surfaces (a) the player's cultivation summary + effective stats, and (b) **every** active buff in one list, aggregated across all three `effectiveStats` sources: timed technique buffs (with remaining seconds), always-on Spirit Card bonuses (stat + meta), and always-on Sect buffs. Two fake-multiplayer social feeds share the same `personas.js` roster as the Pavilion/Sect: a **Rivals** list (asymmetric add/unmark — persona ids on `player.rivals`, round-trips through the existing save with no VERSION bump, lazily back-filled for old saves) and a **Recently Active** feed (deterministic, time-bucketed via the wall-clock pattern — refreshes every ~3 min, weights personas near the player's level, no persistence). Self-contained: owns its own rendering (`initProfile`/`renderProfile`) rather than routing through `ui.js`. GDD §6.5.
- **Achievements / milestones** (`achievements.js`): a 🏆 Achievements modal (13 milestones — first kill, 50/250 kills, first breakthrough, Foundation realm, first technique, Epic+/Legendary+ gear, 1k stones, first disciple, full card set, a maxed card, full codex) with an unlock toast. Every milestone is a **read-only predicate** over existing player state (`bestiary` kills, `level`, owned-item `rarity`, `cards`, `guild.members`, `spiritStones`, `learnedTechniques`) — so the catalog can be re-tuned freely with no migration. `player.achievements` is only a "have we toasted this" ledger (back-filled in `createGame` for old saves). `game.js` `checkAchievements(state)` is a tiny idempotent check-record-persist hook; `main.js` calls it at the top of `renderAll`, so any state change (kill, breakthrough, hire, buy, card, equip) unlocks live and toasts. Self-contained UI (own modal + toasts, never touches `ui.js`); imports the creature/card/realm catalogs read-only (leaf module, no cycle). GDD §6.5.
- **Settings / preferences** (`settings.js`): a single ⚙ Settings modal that consolidates the scattered display prefs + housekeeping into one panel (GDD §6.2) — the **instant-combat toggle** (relocated out of the combat panel), a **Replay tutorial** action, and the **reset save** ("Abandon Cultivation") action. Deliberately low-touch: the relocated controls keep their original ids (`#chk-instant`, `#btn-reset`), so `ui.js`'s `initCombatSettings()` and `main.js`'s reset handler still bind them unchanged — `settings.js` owns only the modal open/close and the one new action (Replay reuses the tutorial's own `❔ Help` button rather than reaching into its internals). No `ui.js` edit, no new save fields.
- **Epic quest chain** (`quests.js` + `items.js`): "The Heaven-Severing Blade" saga — a 5-step endgame chain appended to the existing quest ladder (opens once you've established Foundation), spanning both zones (Bone Serpents in Azuremist → Cindervein altar-reach → Cinder Golem trial → Ashen Revenant vigil → ascend to FE5). Culminates in a hand-authored **Legendary** artifact. Named rewards are a new additive `items.js` concept: `NAMED_ITEMS` (fixed-stat, in-band-for-rarity, **never** in random loot) minted by `mintNamedItem(id)`; a quest reward with an `item.named` key routes through it via a 3-line branch in `game.js` `claimQuest` (else it rolls one as before). Two named items ship: the Epic robe *Ashen Aegis of the Fallen* (mid-chain) and the Legendary weapon *Skyfracture, the Heaven-Severing Blade* (capstone). Pure data + reward-path plumbing — no new UI (reuses the Sect Missions panel), no save-schema change. GDD §5.
- **Visual / UI polish pass** (`css/style.css` + light `index.html`, no `ui.js`/logic touch): the Stage 2 "looks intentional" sweep. Killed the `Stage 1 MVP` dev label (intentional tagline + clean `<title>` + an inline-SVG ☯ favicon that also removes the old `/favicon.ico` 404). Added a small design-token layer (`--radius`, `--shadow-soft`, `--gold-soft`, `--ring`, …) and an appended `polish pass` section that layers over the feature styles without rewriting them: page-wide motion transitions (gated by `prefers-reduced-motion`), a keyboard **`:focus-visible`** gold ring (the app was mouse-only), a **sticky HUD header** with depth, tabular-figure player chips, unified panel elevation, a subtle radial-glow body backdrop, richer per-band map-tile gradients (keeping the green→amber→red danger semantics), and `backdrop-filter` blur + one shared `✕` close-button style across all seven modals. Structure: the six feature buttons became a tidy 2-col **`#nav-menu`** grid, and the **Chronicle** moved under the map to fill the dead space in the left column. Fully **responsive** — centered max-width layout on ultrawide, fluid map + relaxed columns ≤900px, single-column stack + one-per-row nav ≤620px (no horizontal overflow at 380px). Verified in real Chromium (16-check DOM sweep: tutorial steps/closes, all modals open/close, no console errors).

**Stage 3 (1.0) — in progress** (parallel task board on the `coordination` branch, tasks A–V):
- **Crafting & Forge** (`crafting.js`, GDD §5): a ⚒ **Forge** modal to spend spirit stones improving gear three ways — **Reforge** (reroll an artifact's stat values within its rarity/level via `items.js` `reforgeItem`, chasing a better spread), **Temper** (raise its level, scaling every stat up by the same per-level factor `generateItem` uses, so a good roll stays good — capped at `MAX_FORGE_LEVEL` 20), and **Repair** (restore durability from anywhere, not just a haven). Mechanical helpers + rarity-scaled costs (`reforgeCost`/`upgradeCost`) are **additive in `items.js`** (alongside `sellValue`/`repairCost`); game-layer wrappers `forgeReforge`/`forgeUpgrade`/`forgeRepair` spend stones + persist. Self-contained like `loadouts.js`: owns its own ⚒ button (injected into `#nav-menu`) and modal DOM — **no `index.html`, no `ui.js`**. Nothing new persisted (only mutates items already on `player`). GDD §5.
- **Hunt bounties** (`bounties.js`): a 🏹 bounty-board modal (GDD Stage 3) behind a `BountyProvider` interface (`offered`/`active`/`accept`/`claim`/`claimableCount`) — networkable later like market/guild. The *offered* board is generated deterministically from a wall-clock time bucket (the same pattern as the Pavilion/Recently-Active feeds) and rotates every ~15 min, favouring creatures near the player's level. Accepting a bounty snapshots the current `bestiary[typeId].kills`; progress is `kills − snapshot`, read live off existing codex data — so bounties need **no hook in the combat/kill path**, only a read. Accepted bounties persist on the additive `player.bounties` field (round-trips through the save, lazily back-filled, no VERSION bump — like `player.rivals`), don't expire on rotation, cap at 3 active, one per creature. Claim pays bonus spirit stones + XP (scaled to target count × creature value) through the shared `grantXp` path. Self-contained rendering (`initBounties`/`renderBounties` + a claimable-count badge); game-layer wrappers `acceptBounty`/`claimBounty`. Never touches `ui.js`.
- **Sparring / offline PvP-preview** (`rivals.js` + `duel.js`): the PvP hook, still offline (GDD §4.1, §6.5). `rivals.js` synthesizes a **deterministic, level-scaled Actor stat-sheet** for any persona (id → archetype [Striker/Bruiser/Duelist/Balanced] + ±variance, level → scale) in the one-Actor shape `resolveCombat` expects — no persistence, identical every run, like the persona roster itself. `duel.js` is a ⚔ spar modal that runs the player's `playerCombatActor` against a rival's sheet through the **pure `resolveCombat`** (no game state leaks into `combat.js`), shows the matchup + outcome banner + a collapsible turn log, and keeps a win/loss/draw tally on the additive `player.sparRecord` (round-trips through the save, lazily back-filled, no VERSION bump). Entry point is a **"Spar" button on each Profile rival row** — wired via an injected `setSparHandler` callback so `profile.js` takes no `duel.js` import (no cycle). Rival stats headless-tuned so sparring peers/below is winnable when geared, sparring "up" is a real challenge, and under-gearing makes even peers hard — the player self-selects difficulty by which rival they pick. Self-contained rendering; never touches `ui.js`.
- **Daily Trials** (`trials.js`, GDD §5): a 🗓 **Daily Trial** modal — one wall-clock-daily challenge encounter, one attempt per UTC day. The foe is authored here as data: a **level-appropriate benchmark** stat block (deliberately NOT the player's own stats — so better gear/allocation actually help you win, rewarding progression) times a day-chosen difficulty **tier** (Lesser/Even/Greater), deterministic from the UTC day-number + level so it rotates daily and is identical for every cultivator of that level. The fight runs through the **pure `resolveCombat`** against a `playerCombatActor` snapshot; a **win** pays bonus stones/XP (+ a tier-gated chance at gear), a **loss/draw costs nothing** (free daily → "come back tomorrow"). One attempt tracked by a day-number bucket on `player.trials` (`{lastDay, plays, wins}`, back-filled in `createGame`; resets at UTC midnight, offline-safe like Qi regen). `game.js` `attemptDailyTrial(state)` is the only game-layer hook (costs no Qi); self-contained UI (own 🗓 button in `#nav-menu` + modal, never touches `ui.js`), badge shows Ready / countdown. GDD §5.
- **Meridian talent tree** (`meridians.js`, GDD §5): a permanent passive-stat tree, separate from the character-sheet stat points. A ☯ **Meridians** modal opens the eight extraordinary meridians (5 flat-stat nodes ranked to 5 — Governing→Attack, Yang Linking→Defense, Thrusting→Damage, Girdle→Armor, Conception→Max HP). Points are **derived from level** (1 per breakthrough = `level − 1`), not a stored counter — so every existing save immediately has the right number of points and a breakthrough grants one for free, with **no migration and no `applyBreakthroughs`/`game.js` change**. `meridianBonuses(player)` plugs into `progression.js` `effectiveStats` as the **fourth flat source** (one add-line, alongside gear/cards). `player.meridians = { nodes: { id: rank } }` on `createPlayer` (helpers tolerate old saves that lack it). Self-contained like `crafting.js`/`loadouts.js`: owns its own ☯ button (injected into `#nav-menu`) + modal DOM — **no `index.html`, no `ui.js`**; the allocate action persists via `save.js` `saveGame` wired in `main.js`, keeping `game.js` untouched. (Known minor: the character-sheet stat tooltip lumps the meridian bonus into its "gear" figure — the effective total is correct; the Meridians modal shows the real breakdown.) GDD §5.
- **Alchemy / consumables** (`alchemy.js` + `css/alchemy.css`, Stage 3 task C, GDD §6.4): a 🜁 **Alchemy** modal to brew pills from spirit stones (gated by cultivation stage) into a pouch, then quaff them for an **instant** effect (Spirit-Gathering → +200 Qi, Enlightenment → +80×stage XP) or a **timed combat buff** (Ashfury +25% Atk/Dmg, Ironbark +30% Armor/+20% Def, Bloodsurge +30% HP). Pills live on `player.consumables` (`{pillId: qty}`) and active buffs on `player.pillBuffs` (both back-filled in `createGame`, no VERSION bump). **Design note:** technique buffs on `player.activeBuffs` are rendered by `ui.js` via a technique lookup that throws on a non-technique entry — so rather than reach into that shared renderer (UX-session territory), pill combat-buffs live on their **own list** and are applied at **combat time** to the combat-actor snapshot (`applyPillBuffs`, parallel to the debug god-stat hook) in `attack()` and the daily trial — boosting fights without touching `effectiveStats`/`ui.js`/`techniques.js`. Game-layer wrappers `brewPill`/`usePill`/`tickPillBuffs` own the economy + expiry; a dedicated **HUD bar** under the header (own DOM) shows active pill buffs with live countdowns. No new material item type (avoids colliding with a future Salvage material shape). Fully self-contained — never touches `ui.js`. GDD §6.4.
- **Combat feedback / "juice"** (`combatfx.js` + `css/combatfx.css`, Stage 3 task W): a purely-presentational layer over combat playback. Reads the already-resolved `result.turns[]` (mutates **no** game state) and dresses each turn — floating **damage / crit / MISS** numbers over the struck actor, a hit-flash + subtle shake, a two-actor **arena with HP bars that tween down** turn-by-turn, and a **victory/defeat/draw flourish** on resolution. Owns all its own DOM (an arena injected into `#combat-panel`) and its **own stylesheet** (linked in `<head>`, never appended to `style.css`); `ui.js` touch is three one-line hooks in `playCombat` — `beginFx(result, isInstant())` after the title, `turnFx(result.turns[i])` as each log line renders, `endFx(result)` in `finish()`. Player start-HP is derived from turn 1 (no game-state read); instant-resolution mode is bypassed (no arena); all motion gated behind `prefers-reduced-motion` (numbers still show).

- **Second Legendary boss + boss registry** (`boss.js`, GDD §5/§9.1): refactored `boss.js` from a single `BOSS` const into a data-driven **`BOSSES` registry** (+ `BOSS_LIST`) so bosses are data — every function (`maybeManifestBoss`/`onBossDefeated`/`bossLairStatus`/`bossHints`/`spawnBoss`) now operates over the registry, and a new calamity is a new data block + its card, no new logic. Added boss #2 — **Zhulong, the Ember Calamity** (a flame-calamity torch-dragon): its own lair (Cindervein `(0,9)`, opposite corner from the Ancient Terror), gated at **Foundation Establishment 5** (`minStage 14`), a longer 45-min cooldown, guaranteed **Epic (30% Legendary)** drop, and a boss Spirit Card (`card_emberCalamity`, +3 Attack/level). Hand stat block **ATK 53 · DEF 45 · DMG 35 · ARM 23 · HP 508 @ Lv 18**, headless-tuned so FE5 = crushed, FE7 = a ~46% win/draw/loss gamble, maxed FE9 = reliable win (the Ancient Terror's curve shifted up ~4 levels). Per-boss progress is keyed on `player.boss` by boss id (`{ [id]: {defeatedAt, defeats, hintShown} }`); the old single-boss `{defeatedAt, defeats}` shape is migrated forward on load (`normalizeBossState`, no VERSION bump). `game.js`/`ui.js` boss touches (manifest, lair marker, tile row, `bossCodexEntry`, hint) generalized from one boss to N — the codex now shows a **Legendary Calamities** section with an entry per boss. GDD §5.

- **Unified toast / feedback system** (`toast.js` + `css/toast.css`): one queue-based API `toast(message, type)` (`type ∈ {info, success, warn, error}`) that surfaces the high-signal moments previously buried in the Chronicle log — loot drops, Spirit Cards, breakthroughs, Pavilion purchases, bounty accept/claim, and **errors** ("not enough spirit stones", "need Qi to move"). Queue caps at 3 concurrent (rest pump in as slots free), auto-dismiss ~3.5s, click to dismiss early, `textContent`-only (never interprets a message as HTML). Anchored **top-centre** in its own `#toast-host`; deliberately **shares the achievements-toast visual language but sits at a different corner** so the two never overlap (achievements' bottom-right "unlock" toasts are left untouched — they're out of this module's scope). Own stylesheet linked from `<head>` (not appended to `style.css`); no save fields. Wired at a few clean callsites in `main.js` (combat rewards in `onAttack`, move errors in `onTileClick`, market-buy + bounty handlers); never touches `ui.js`.
- **Fight replay & share** (`replay.js` + `css/replay.css`, Stage 3 task T): the combat `result` is already a complete, self-contained record (`turns[]` + foe + outcome + rewards), so a **replay** is just re-running the existing `playCombat` over the stored result and a **share** is a plain-text transcript of the same data. Additive + presentational: mutates **no** game state, persists the last fight under its own localStorage key (`fallen-immortal-lastfight`, JSON-snapshotted to trim live-only fields — not the save schema), and **injects its own controls** into the combat panel's `.combat-buttons` row (⟳ Replay / ⧉ Share log) plus a share modal — so it **never touches `ui.js`**. `main.js` records the fight in `onAttack` and drives replay through a shared `runPlayback` helper (controls hidden during live playback, shown when a fight finishes); replaying reuses the task-W combat-fx layer for free. `exportLog()` builds the shareable transcript.

- **Statistics / lifetime summary** (`stats.js` + `css/stats.css`, Stage 3 task S3): a read-only 📊 **Chronicle of Deeds** modal grouping Cultivation / Battle / Collection / Fortune / Journey. Most figures are **derived live** from existing save data (realm+stage via `stageName`/`realmFor`, total kills + species faced from `bestiary`, codex/card completion vs the live `CREATURE_TYPES`/`CARDS` catalogs, achievements via `achievementProgress`, stones on hand, sect size) — so they can't drift out of sync. Only the handful that can't be derived are genuine lifetime counters on the additive `player.stats` (`fightsWon/Lost/Drawn`, `stonesWon`, `itemsLooted`, `msPlayed`), incremented at the existing combat outcome hooks in `game.js` `attack()` (self-defending `st.x = (st.x||0)+…` lines) and via `tickPlaytime(state)` on the world tick — which accrues **active** time only (ignores >5s gaps so backgrounded/offline time isn't counted). `player.stats` is `{}` on `createPlayer` + lazily back-filled in `createGame` (no VERSION bump). Self-contained like `crafting.js`: injects its own 📊 button into `#nav-menu` + owns its modal DOM, own stylesheet — **no `ui.js`**, never mutates state. GDD Stage 3.
- **World events / calendar** (`events.js` + `css/events.css`, Stage 3 task R): a deterministic, repeating **wall-clock calendar** of global buffs — the active event is a pure function of the clock (`bucket = floor(now / 20min)`, `EVENTS[bucket % n]`), like the Pavilion rotation and Recently-Active feed, so it needs **no persistence** and is identical for everyone at a given moment. Five rotating windows: a neutral **Serene Skies** (so buffs feel like an occasion) plus **Enlightenment Tide** (+50% XP), **Spirit Torrent** (+50% stones), **Heaven's Bounty** (+25%/+25%), **Blood Moon** (+75% stones). The game layer applies the multipliers in **one guarded spot** in `attack()`'s win branch (`eventReward()` → `xp/stones *= mult`, covering boss + normal kills; a no-op during the calm window). A slim **HUD banner** (injected under the header, own DOM) shows the active event + a live countdown, refreshed on the existing 1s tick. Leaf module — game.js reads the multiplier, `main.js` drives the banner; purely additive, mutates/persists nothing.
- **Theme system (light/dark)** (`theme.js` + `css/theme.css`): a palette switch driven entirely by CSS custom properties. Dark "parchment" stays the default (defined in `style.css` `:root`); the toggle flips a `data-theme` attribute on `<html>` and `theme.css` (loaded after `style.css`) supplies the **light-palette token overrides** under `:root[data-theme="light"]` — so everything reading a `var(--…)` reskins for free — plus a few **targeted surface fixes** for elements that hardcode a colour (control fills, the combat-log inset, the player glyph, the body backdrop gradient) which token overrides alone can't reach. The choice is a **display preference, not save state** — its own localStorage key (`fallen-immortal-theme`), never in the save schema, so it survives export/import/reset. A tiny inline `<head>` bootstrap sets `data-theme` **before paint** (no flash); `theme.js` re-applies idempotently and injects the **Appearance toggle into the ⚙ Settings modal** (low-touch: `settings.js` calls `initThemeControl()`; the toggle self-builds, reusing the existing settings-section/toggle classes). No `ui.js` touch, no `style.css` edit (light overrides live in `theme.css`), no save fields. GDD §9.4.

- **Accessibility & keyboard input** (`input.js` + `css/a11y.css`): makes the game keyboard-playable and screen-reader-friendlier without touching combat/game logic. **Arrow keys / WASD** move to the adjacent tile (reuses `main.js`'s existing `onTileClick` → `tryMove`, no duplication); **Esc** closes the top-most open modal (mirrors scrim-click, hiding the last visible `#*-overlay`); **digit keys 1–9** open the Nth `#nav-menu` panel (queried live, so runtime-injected Forge/Meridians/Trials buttons are included). Guards: ignores keys while typing in a field or with Ctrl/Meta/Alt held, and suppresses map movement while a modal is open (Esc still closes). `applyA11y()` sets `role="group"` + a keyboard-hint `aria-label` + `tabindex="-1"` on `#map-grid` (skip-link target) and `role="dialog"`/`aria-modal`/derived `aria-label` on every overlay — done in JS so runtime-injected modals are covered. `a11y.css` adds the skip link, an `.sr-only` utility, and a keyboard-only `.tile:focus-visible` ring (own sheet, linked in `<head>` — no `style.css` edit). Only the `move` callback is game-coupled; `main.js` wires it in one line (`initInput({ move })`), last so every modal exists for the ARIA pass. Never touches `ui.js`/`combat.js`. Keyboard-only — touch/mobile stays a separate module (task Z). GDD §9.4.

- **Item comparison tooltips + inventory UX** (`itemcompare.js` + `css/itemcompare.css`, board task Y): hovering an *unequipped* artifact now appends a **delta block vs the piece worn in that slot** — ▲ green upgrade / ▼ red downgrade / – grey neutral per stat — so "is this better?" reads at a glance (empty slot → "all new"). Pure **read** of `player.equipment` (never mutates state); `setCompareContext(player)` is set from `renderGear` and `compareRows(item)` is the single line added to `ui.js`'s shared `itemTooltip`. The owned sheet (loaded after `style.css`, linked in `<head>`) also layers on rarity-tinted slot **glow**, a green ✓ **equipped marker** on the two equipment slots, and a tidier right-click menu — all pure CSS over classes `ui.js` already emits, so no extra `ui.js`/`style.css` churn. GDD UX polish.
- **Sect disciple missions** (`sectmissions.js` + `css/sectmissions.css`): a 🗺 **Sect Dispatch** modal that sends hired disciples on **timed wall-clock missions** (Gather 20m / Patrol 45m / Escort 90m) that return spirit stones + XP. Behind a `SectMissionProvider` interface (`roster`/`assign`/`resolveDue`/`tray`/`collect`/`collectableCount`), created in `createGame` like the market/bounty providers. Reads the roster via the existing `state.guildProvider.getMembers()` — **no `guild.js` edit**. Reward scales with disciple level × mission length. A mission finishing is `Date.now() >= endsAt`, so it resolves correctly even offline (same pattern as Qi regen / the Pavilion): `game.js` `tickSectMissions` (called from the per-second loop) moves finished missions to a **returned tray**, and `collectSectMissions` pays the tray out through the shared `grantXp`/stone path. Active missions + tray persist on the additive `player.sectMissions` field (`{active, tray}`, lazily back-filled, no VERSION bump). Self-contained like `crafting.js`: injects its own 🗺 button (into `#nav-menu`), modal DOM, **and its own stylesheet** (`<link>` appended at init) — **no `index.html`, no `ui.js`**; game-layer wrappers `startSectMission`/`tickSectMissions`/`collectSectMissions`, badge shows collectable count. GDD §4.3.

- **Mobile / touch & responsive overhaul** (`css/responsive.css`, board task Z): a phone-first pass layered over the polish pass's ≤900/≤620 breakpoints. Linked **last** in `<head>` so its media rules win. On ≤620px the feature-button `#nav-menu` becomes a **fixed bottom nav dock** (thumb-reachable, horizontal-scrolls, safe-area padded) reusing the existing buttons — no new markup; `body` gets padding to clear it. Bigger tap targets (≥40–44px), `touch-action: manipulation` (kills the 300ms tap delay), `env(safe-area-inset-*)` for notches, and **zero horizontal overflow 320→900px** — the key fixes being `.tile { min-width: 0 }` (the inline `repeat(N,1fr)` map grid couldn't shrink otherwise) and an `auto-fill` pack grid in the 621–900 tablet band. Modals use `[id$="-overlay"]`/`[id$="-panel"]` generic selectors so every current + future modal adapts. Presentation-only: **no `js`/`ui.js`** (tiles/nav are already `<button>`s, so tap = click and tap-to-move works natively); `index.html` touch is just `viewport-fit=cover` + the `<link>`. GDD §9.4 / UX.

- **Gem sockets / enchanting** (`sockets.js` + `css/sockets.css`, board task U, GDD §5): **Rare+** gear now rolls empty **sockets** (Rare 1 · Epic/Legendary 2 · Mythic 3); hunting drops loose **gems** (one per combat stat — e.g. Bloodstar Ruby→Attack, Verdant Spirit-Jade→Max HP), and slotting a gem into a socket adds its flat stat. `socketBonuses(player)` plugs into `progression.js` `effectiveStats` as the **fifth flat source** (one add-line, after gear/cards/meridians; honours the broken-gear rule — a broken item's gems lie dormant). Split to stay parallel-safe: `sockets.js` **owns** the gem catalog, socket-count table, gem generation, the aggregation, the slot/unslot actions and its own 💎 **Jewelcraft** modal (button injected into `#nav-menu`); `items.js` only asks two pure questions (`socketCountFor` on generate, `generateGem` inside `rollDrop` so gems ride the existing loot/inventory/sell path — imports one-directional, no cycle). `ui.js` touches are in the **same shared item-tooltip region as task Y** (kept in one session): gems render with a gem icon + no durability bar and don't equip on click, and `socketLine(item)` lists an item's sockets in its tooltip. Gems carry `id/name/rarity/level` so they sell/destroy/save like any item; a `gem` counter is persisted alongside `item`/`creature` in `save.js` for unique ids (additive, old saves default 0). Sockets live on the item object (`item.sockets`), so they round-trip through the save and survive equip-swaps/reforge/temper untouched. GDD §5.

- **Core Formation realm + advanced techniques** (`progression.js` + `techniques.js`, GDD §9.1): the **3rd realm**. `REALMS` gains **Core Formation** (9 stages), so `MAX_STAGE` is now **27** (CF1 = level 19 … CF9 = 27) — `realmFor`/`stageName`/`applyBreakthroughs` pick it up automatically since the ladder is pure data. `STAGE_XP` extends with the **FE9→CF1 barrier spike** (200k, ~×2.2 over FE8→9, matching the QC9→FE1 bottleneck shape) then the CF1→9 climb (~×1.4 per step to 2.65M). New **tier-4** capstone techniques gated at CF1 (`minStage 19`), one per category, each requiring its tier-3 art: **Void-Rending Fist** (Offense, +40% Atk/+35% Dmg), **Immortal Golden Body** (Defense, +25% Def/+40% Arm/+30% HP), **Golden Core Ascendance** (Special, +22% to all four) — plus a bridging Special **tier-3 Spirit-Severing Palm** (FE5) so Special has a full 1→4 chain. Curve headless-verified (monotonic, both barriers spike, CF stage names). The `REALMS` addition is a clean isolated hunk so the third-zone task (F) can gate its portal on the new realm without conflict. GDD §9.1.
- **Gear set bonuses** (`sets.js` + `css/sets.css`, board task B, GDD §5): matched weapon+robe pairs share a `setId`; wearing the whole set grants a flat **set bonus** on top of the pieces' own stats. Three sets across the tiers — **Skydancer Regalia** (Rare: Cloudpiercer Jian + Crane-Feather Cloak → +4 Atk/+4 Dmg), **Phoenix Ascendant** (Epic → +8 Dmg/+20 HP/+4 Armor), **Nine Heavens Calamity** (Legendary → +10 Atk/Def/Dmg, +30 HP). `setBonuses(player)` plugs into `progression.js` `effectiveStats` as the **sixth flat source** (one add-line, after sockets; honours the broken-gear rule — a broken piece doesn't count toward its set). Threshold model (`bonusAt: { count: {stats} }`) so larger sets / a future 3rd slot just add higher thresholds, no code change. `sets.js` **owns** the catalog, aggregation and the tooltip helper; `items.js` only adds `setId` to the six member templates + copies it onto generated items (additive, no roll-range change); `ui.js` shows set progress in the **same shared item-tooltip region as Y/U** — `setSetsContext(p)` from `renderGear` + `${setLine(item)}` in `itemTooltip` (owned/equipped ticks, live X/N progress, bonus highlighted once complete). `setId` lives on the item object, so it rides the save/equip-swap/reforge/temper pipelines untouched; imports one-directional (no cycle). GDD §5.
- **Ascension / New Game+** (`ascension.js` + `css/ascension.css`, GDD §5): a prestige loop. At the **peak of cultivation** (`player.level >= MAX_STAGE`, so it tracks whatever the top realm is) a ✦ **Ascension** modal offers to shatter your foundation and begin anew — `performAscension` **wipes** the run (level/xp, allocated stats, meridians, techniques, all gear + loadouts, stones→starting purse) but **keeps** collections/meta (Spirit Cards, Beast Codex, Sect, rivals/spar, achievements, lifetime stats) and increments `player.ascension`. Each tier grants a **permanent +8% to every effective stat**, applied as the final global scalar in `progression.js` `effectiveStats` — read straight off `player.ascension` (a plain integer, `?? 0`) so **progression.js needs no `ascension.js` import** (no module cycle); the shared constant `ASCENSION_STAT_PER_TIER` lives in `progression.js`. `player.ascension` is additive (added to `createPlayer`, `?? 0` everywhere), no VERSION bump. Self-contained like `crafting.js`/`sectmissions.js`: injects its own ✦ button + modal + stylesheet — **no `index.html`, no `ui.js`**. `game.js` `ascend(state)` is the thin wrapper (resets Qi to the new cap, logs, saves) alongside `canAscendNow`; `main.js` wires it. GDD §5.

- **Navigation & HUD redesign** (`css/hud.css`, board task AB): the top HUD's flat stat-chips become **labelled stat tiles** — Realm / Vitality / Spirit Stones / Points as value tiles, and **Qi + Breakthrough as real meters** (a filled track with the value centred over it). The `chip-*` ids are **kept as the value/text spans** so `renderPlayerBar`'s bindings and the tutorial spotlight (`#chip-qi`) are unchanged; `renderPlayerBar` also drives two new fill elements (`bar-qi` / `bar-xp`) whose widths are `qi/maxQi` and `xp/need` (`atPeak → 100%`). The `#nav-menu` button-grid becomes a titled, `auto-fit` **dock** (a full-width `.nav-dock-title` heading + uniform tiles) that works for both the static buttons and the ones modules inject at runtime (Forge/Meridians/Jewelcraft/Chronicle/Sect-Dispatch) — the heading is a non-`button` child so `input.js`'s `#nav-menu button` digit-nav is unaffected. Own sheet **linked before `css/responsive.css`** so task Z's ≤620px fixed bottom nav dock still wins; on mobile the dock title hides and the HUD tiles tighten. Presentation + one shared hook: `renderPlayerBar` gains the meter-fill lines (`ui.js`), `index.html` restructures `#player-bar` + adds the dock title; **`main.js` was not needed** (meters ride the existing `renderPlayerBar` calls). No `style.css` edit. (Pre-existing, out of scope: a ~15px desktop horizontal overflow from the fixed-54px inventory grid exists on `master` too — flagged for a follow-up, not introduced here.) GDD §9.4.

Next: remaining Stage 3 tasks on the board (B–V), incl. the UX overhaul batch (W–AB). **Strip the testing conveniences** (Stage-2 PR #13) stays **held until Stage 3 dev is done** — it removes the debug tooling Stage-3 sessions rely on.

## Architecture conventions (hold these)

- **`combat.js` is a pure function** `resolveCombat(attacker, defender, seed)` — deterministic, never touches game state/UI/rewards. It's what a future PvP mode calls against a player's stat sheet. Don't leak game logic into it.
- **One Actor shape** for player/monsters/(future rivals). The persistent player splits `base` / `allocated` / `equipment` / buffs; combat stats come from `playerCombatActor()`.
- **Single stat-aggregation pipeline** `effectiveStats(player, now)` = base + trained + gear + Spirit Cards (flat) then technique buffs (percentage). Never mutate stats in place on equip/cast/drop. New passive sources (sect buffs, set bonuses) plug in here.
- **Providers behind interfaces**, NPC-backed now, networkable later (market/guild/events). `createMarketProvider(state)` (`market.js`) and `createGuildProvider(state)` (`guild.js`) are the references: each returns its GDD method set, and `game.js` only ever calls the interface — a 2.0 network impl swaps in without touching callers. Build the interface even for the stub.
- **Modular files**, no monolith: `combat` `actors` `progression` `items` `quests` `techniques` `cards` `market` `personas` `guild` `boss` `tutorial` `loadouts` `profile` `achievements` `settings` `crafting` `bounties` `rivals` `duel` `trials` `meridians` `combatfx` `toast` `replay` `theme` `stats` `events` `input` `itemcompare` `alchemy` `sectmissions` `sockets` `sets` `ascension` `map` `save` `game` `ui` `main` (+ `rng`).
- **World data is per-zone** (task E): each zone lives in its own file `js/zones/<id>.js` exporting `ZONE` (grid/danger/spawns/portals) **and** `CREATURES` (the creature templates native to it). `js/zones/registry.js` composes them into the global `ZONES` + `CREATURE_TYPES`; `map.js` re-exports `ZONES` and `actors.js` re-exports `CREATURE_TYPES`, so every existing `import { ZONES } from './map.js'` / `import { CREATURE_TYPES } from './actors.js'` is unchanged. `map.js` is loader + grid logic only; `actors.js` is the Actor schema + spawn helpers. **To add a zone: create one `js/zones/<id>.js` and add it to `ZONE_MODULES` in `registry.js` — nothing else changes.** Bosses stay separate (`boss.js` owns `BOSSES`, its own typeIds never in a zone spawn table).
- **Save is the only "account"** (`save.js`, localStorage, versioned). Bump `VERSION` and add a migration branch for breaking shape changes — v1→v2 migration is the reference. Persist timestamps (`lastQiTick`, `lastStoneTick`, buff `expiresAt`) and world state (`market`) so wall-clock/offline behavior is free. New fields (`player.cards`, `player.guild`, `lastStoneTick`, `market`) are back-filled in `createGame` for old saves — no VERSION bump needed for additive shape changes. Deterministic-from-seed data (the `personas.js` roster) is regenerated, never stored.
- **Wall-clock timing pattern**: elapsed-time math (`Date.now()` vs stored timestamp) for Qi regen, passive spirit-stone income (`tickStones`), buff expiry, and Pavilion listing rotation + async sale resolution (`tickMarket`) — survives reload/offline identically.

## TESTING-ONLY — strip before demo

All marked `TESTING ONLY` in code; in `game.js` `grantTestingKit()` and constants:
- `MAX_QI = 500` (`game.js`) → lower for real tuning.
- Quartermaster kit: one item of every rarity + `+8 technique / +6 stat points` + full Qi (re-runs when `TEST_KIT_VERSION` bumps).
- `INVENTORY_SIZE = 24` (`items.js`) → small starting size per GDD §6.2.
- **Debug panel** (`js/debug.js` — the dashed purple "🛠 Debug" box at the bottom of the character column): God mode (one-shot / never die), item- & card-drop-rate multipliers, spawn any creature at any level on the current tile, mint gear of any rarity/slot/level, grant stones/XP/breakthrough/points/Qi, grant/max/clear Spirit Cards, reveal the whole codex, and rotate/resolve the market. **To strip:** delete `js/debug.js`; remove its `import`/`initDebug()` in `main.js`; remove the `TESTING ONLY — debug tools` block in `game.js` plus the `applyGodStats(actor)` call in `attack()`; and remove `setDropMultiplier`/`setCardDropMultiplier` (and their use in `rollDrop`/`rollCardDrop`) in `items.js`/`cards.js`. All are labelled `TESTING ONLY`.

## Workflow

- One commit per completed task; **update the "Current status" section above in that same commit.**
- Commit messages end with the `Co-Authored-By: Claude` trailer.
- CRLF warnings from git on Windows are expected/harmless.
